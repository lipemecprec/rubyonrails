<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.id %>
      <a href="/articles/<%= article.id %>">
       <%= article.title %>
      </a>   
      <%= article.body %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>